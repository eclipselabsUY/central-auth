# API Overview

The service currently exposes three public endpoints.

## Endpoints

- `GET /`
- `GET /health`
- `POST /login`

See the endpoint pages for request/response details and behavior notes.
